#include <bits/stdc++.h>
using namespace std;

int main(){
    pair<long long, long long> p;
    cin >> p.first >> p.second;

    cout << p.first + p.second << "\n";
    cout << p.first - p.second << "\n";
}
